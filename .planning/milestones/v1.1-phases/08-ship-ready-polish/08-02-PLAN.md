---
phase: 08-ship-ready-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements:
  - SHIP-03

must_haves:
  truths:
    - "All app routes respond without errors: quotation, invoice, receipt, delivery-note, documents, bank-reconciliation"
    - "PDF generation works for quotation and invoice with THSarabunNew font"
    - "Thai text displays correctly throughout the UI without encoding issues"
  artifacts: []
  key_links:
    - from: "app/(app)/apps/quotation/page.tsx"
      to: "/apps/quotation"
      via: "Next.js app router"
      pattern: "quotation"
    - from: "app/(app)/apps/bank-reconciliation/page.tsx"
      to: "/apps/bank-reconciliation"
      via: "Next.js app router"
      pattern: "bank-reconciliation"
---

<objective>
Visual QA of BanChee with real Thai workflows -- verify all app routes respond, PDFs render with correct fonts, and Thai text displays properly.

Purpose: Confirm that all v1.1 features work correctly before shipping.
Output: Verified working app with all routes, PDFs, and Thai text rendering.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-ship-ready-polish/08-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated route and functionality smoke test</name>
  <files></files>
  <read_first>
    - app/(app)/apps/quotation/page.tsx (route exists)
    - app/(app)/apps/invoice/page.tsx (route exists)
    - app/(app)/apps/receipt/page.tsx (route exists)
    - app/(app)/apps/delivery-note/page.tsx (route exists)
    - app/(app)/apps/documents/page.tsx (route exists)
    - app/(app)/apps/bank-reconciliation/page.tsx (route exists)
  </read_first>
  <action>
    Per D-08, D-09, D-10, D-11:

    **Start the dev server** (D-08 -- use dev server, no live LLM needed):
    - Run `npm run dev` in background
    - Wait for server to be ready at http://localhost:7331

    **Test all app routes respond (D-09):**
    Use curl to verify each route returns HTTP 200 (or 3xx redirect to self-hosted setup on first run):
    - http://localhost:7331/apps/quotation
    - http://localhost:7331/apps/invoice
    - http://localhost:7331/apps/receipt
    - http://localhost:7331/apps/delivery-note
    - http://localhost:7331/apps/documents
    - http://localhost:7331/apps/bank-reconciliation
    - http://localhost:7331/dashboard
    - http://localhost:7331/transactions

    Record HTTP status codes for each route. All should return 200 or 302 (redirect to auth/setup).

    **Verify Thai text rendering (D-11):**
    - Curl one of the pages and grep for Thai characters to confirm they render (not mojibake)
    - Check that the response HTML contains UTF-8 charset declaration

    **Verify PDF font setup (D-10):**
    - Check that THSarabunNew font files exist in the expected location (public/fonts/ or similar)
    - Confirm the PDF components reference THSarabunNew

    Stop the dev server after testing.
  </action>
  <verify>
    <automated>curl -s -o /dev/null -w "%{http_code}" http://localhost:7331/apps/quotation 2>/dev/null | grep -E "200|302" && echo "ROUTE_OK" || echo "ROUTE_FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - All 8 routes return HTTP 200 or 302
    - Thai characters present in HTML responses (not mojibake)
    - THSarabunNew font files exist in the project
    - Dev server starts without compilation errors
  </acceptance_criteria>
  <done>All app routes respond, Thai text renders correctly, font files confirmed present</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of BanChee UI and PDFs</name>
  <files></files>
  <action>
    Present the verification checklist to the user for manual visual inspection.
    Dev server must be running at http://localhost:7331 from Task 1.

    Verification steps for the user:
    1. Dashboard: Visit http://localhost:7331/dashboard -- verify Thai labels render correctly
    2. Quotation flow: Visit http://localhost:7331/apps/quotation -- create a test quotation, verify PDF download renders THSarabunNew font and Thai text
    3. Invoice: Visit http://localhost:7331/apps/invoice -- verify the list page loads
    4. Receipt: Visit http://localhost:7331/apps/receipt -- verify the list page loads
    5. Delivery note: Visit http://localhost:7331/apps/delivery-note -- verify the list page loads
    6. Unified documents: Visit http://localhost:7331/apps/documents -- verify filter tabs work
    7. Bank reconciliation: Visit http://localhost:7331/apps/bank-reconciliation -- verify import page loads
    8. PDF rendering: Download any generated PDF and confirm THSarabunNew font, correct Thai text, proper layout
  </action>
  <verify>User types "approved" or describes issues</verify>
  <acceptance_criteria>
    - User confirms all routes load without errors
    - User confirms Thai text renders correctly (no mojibake)
    - User confirms PDF generation produces readable Thai documents with THSarabunNew
  </acceptance_criteria>
  <done>Human visual approval of all v1.1 features -- routes, Thai text, PDF rendering</done>
</task>

</tasks>

<verification>
- All 8 app routes return HTTP 200 or 302
- Visual confirmation of Thai text rendering
- PDF generation produces readable Thai documents
</verification>

<success_criteria>
- Every v1.1 app route responds without errors
- Thai text displays correctly across all pages
- PDF generation works with THSarabunNew font
- Human visual approval of overall UI quality
</success_criteria>

<output>
After completion, create `.planning/phases/08-ship-ready-polish/08-02-SUMMARY.md`
</output>
