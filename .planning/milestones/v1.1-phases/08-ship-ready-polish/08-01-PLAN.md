---
phase: 08-ship-ready-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - .env.example
  - docker-compose.yml
  - docker-compose.build.yml
  - Dockerfile
autonomous: true
requirements:
  - SHIP-01
  - SHIP-02

must_haves:
  truths:
    - "README.md describes BanChee (not TaxHacker) with Thai + English sections"
    - "README includes installation guide using Docker with correct image/repo references"
    - "README includes feature overview covering v1.0 AI scanning and v1.1 document workflow + bank reconciliation"
    - "Docker build completes without errors including new Prisma models (Document, BankStatement, BankEntry)"
    - "Docker compose up starts the app and serves at port 7331"
  artifacts:
    - path: "README.md"
      provides: "BanChee project README with Thai + English content"
      min_lines: 120
    - path: ".env.example"
      provides: "Updated env example with BanChee references"
    - path: "docker-compose.yml"
      provides: "Self-hosted docker compose with BanChee image reference"
    - path: "docker-compose.build.yml"
      provides: "Local build docker compose with BanChee references"
  key_links:
    - from: "README.md"
      to: "docker-compose.yml"
      via: "installation instructions reference compose file"
      pattern: "docker.compose"
    - from: "docker-compose.build.yml"
      to: "Dockerfile"
      via: "build context references Dockerfile"
      pattern: "dockerfile.*Dockerfile"
---

<objective>
Rebrand README from TaxHacker to BanChee and verify Docker production build works end-to-end.

Purpose: Make BanChee presentable to the public with accurate documentation and a working containerized deployment.
Output: Rewritten README.md, updated Docker references, verified Docker build.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@README.md
@Dockerfile
@docker-compose.yml
@docker-compose.build.yml
@docker-entrypoint.sh
@.env.example
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebrand README.md and .env.example from TaxHacker to BanChee</name>
  <files>README.md, .env.example</files>
  <read_first>
    - README.md (current TaxHacker README -- full rewrite needed)
    - .env.example (TaxHacker references in comments)
    - .planning/PROJECT.md (core value statement, constraints)
    - .planning/REQUIREMENTS.md (SHIP-01 spec)
    - .planning/phases/08-ship-ready-polish/08-CONTEXT.md (decisions D-01 through D-04)
  </read_first>
  <action>
    Rewrite README.md completely per D-01, D-02, D-03, D-04:

    **Structure (per D-02):**
    1. Logo + title: BanChee (บัญชี) with tagline "AI-powered Thai SME tax accounting"
    2. Badges: GitHub stars, license (MIT), issues -- pointing to mingrath/banchee per D-04
    3. Thai section (primary per D-01): 2-3 paragraph description in Thai explaining what BanChee does for Thai SME owners
    4. English section: Same content in English
    5. Feature Overview:
       - v1.0 features: AI receipt scanning, VAT/WHT calculation, CIT estimation, tax filing deadlines, export
       - v1.1 features: Quotation-to-invoice-to-receipt workflow, bank reconciliation, PDF generation with THSarabunNew
    6. Quick Start (per CONTEXT specifics): Docker one-liner for impatient users
    7. Installation Guide:
       - Docker self-hosted: `docker-compose.yml` with pre-built image
       - Docker local build: `docker-compose.build.yml`
       - Local development: Node.js + PostgreSQL + Ghostscript/GraphicsMagick
    8. Environment Variables: Table of all env vars from .env.example
    9. Thai Tax Terminology Reference: Brief table of key Thai tax terms (VAT/ภาษีมูลค่าเพิ่ม, WHT/ภาษีหัก ณ ที่จ่าย, CIT/ภาษีเงินได้นิติบุคคล)
    10. Screenshots: Use placeholder text like `<!-- TODO: Add screenshots -->` for now
    11. License: MIT, with attribution to TaxHacker as upstream fork per D-03
    12. Credits: "Built on [TaxHacker](https://github.com/vas3k/TaxHacker) by vas3k"

    **Also update .env.example:**
    - Change comment references from TaxHacker to BanChee
    - Update RESEND_FROM_EMAIL default from "TaxHacker" to "BanChee"
    - Keep all env var names unchanged (backward compatible)
  </action>
  <verify>
    <automated>grep -c "TaxHacker" README.md | xargs test 0 -eq && grep -q "BanChee" README.md && grep -q "mingrath/banchee" README.md && grep -q "MIT" README.md && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - README.md contains zero occurrences of "TaxHacker" as the project name (attribution in credits/license section is acceptable)
    - README has both Thai and English content sections
    - README references mingrath/banchee as the GitHub repo
    - README includes Docker installation instructions
    - README includes feature overview for both v1.0 and v1.1
    - README includes MIT license with TaxHacker attribution
    - .env.example comments reference BanChee not TaxHacker
  </acceptance_criteria>
  <done>README.md fully rebranded to BanChee with Thai+English sections, installation guide, feature overview, and upstream attribution</done>
</task>

<task type="auto">
  <name>Task 2: Update Docker files and verify production build</name>
  <files>docker-compose.yml, docker-compose.build.yml, Dockerfile</files>
  <read_first>
    - Dockerfile (current multi-stage build)
    - docker-compose.yml (self-hosted setup with ghcr.io/vas3k/taxhacker image)
    - docker-compose.build.yml (local build variant)
    - docker-entrypoint.sh (startup script)
    - prisma/schema.prisma (confirm Document, BankStatement, BankEntry models exist)
  </read_first>
  <action>
    Per D-05, D-06, D-07:

    **Update docker-compose.yml:**
    - Change image from `ghcr.io/vas3k/taxhacker:latest` to `ghcr.io/mingrath/banchee:latest`
    - Change database name from `taxhacker` to `banchee` in DATABASE_URL and POSTGRES_DB
    - Update pgdata volume comment if any

    **Update docker-compose.build.yml:**
    - Change container_name from `taxhacker-postgres` to `banchee-postgres`
    - Change database name from `taxhacker` to `banchee` in DATABASE_URL and POSTGRES_DB

    **Verify Dockerfile handles v1.1 models (D-06):**
    - Confirm `COPY --from=builder /app/prisma ./prisma` copies schema with Document, BankStatement, BankEntry
    - Confirm `COPY --from=builder /app/node_modules ./node_modules` includes exceljs (D-07)
    - No Dockerfile changes needed if above is already handled by the existing COPY commands

    **Verify Docker build (D-05):**
    - Run `docker build -t banchee:test .` to confirm the image builds successfully
    - Check that the build completes without errors (especially `npm run build` stage which compiles all v1.1 code)
    - The successful build confirms all v1.1 Prisma models, exceljs, and new app routes are included

    Note: Do NOT run `docker compose up` (requires PostgreSQL and takes too long for CI). The build step itself validates that all code compiles and dependencies resolve.
  </action>
  <verify>
    <automated>docker build -t banchee:test . 2>&1 | tail -5 && echo "BUILD_OK" || echo "BUILD_FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - docker-compose.yml references banchee image, not taxhacker
    - docker-compose.build.yml references banchee database name
    - Docker build completes successfully (exit code 0)
    - Build output confirms Next.js compilation of all routes including v1.1 apps
  </acceptance_criteria>
  <done>Docker files rebranded to BanChee and production build verified end-to-end</done>
</task>

</tasks>

<verification>
- `grep -r "taxhacker" README.md docker-compose.yml docker-compose.build.yml .env.example` returns only upstream attribution lines
- `docker build -t banchee:test .` exits 0
- README.md has Thai content (grep for Thai characters)
</verification>

<success_criteria>
- README.md fully rebranded with Thai+English sections, feature overview, installation guide
- Docker files reference BanChee/mingrath not TaxHacker/vas3k
- Docker production build completes without errors
</success_criteria>

<output>
After completion, create `.planning/phases/08-ship-ready-polish/08-01-SUMMARY.md`
</output>
