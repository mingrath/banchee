---
phase: 07-bank-reconciliation
plan: 03
type: execute
wave: 3
depends_on: ["07-02"]
files_modified:
  - app/(app)/apps/bank-reconciliation/import/page.tsx
  - app/(app)/apps/bank-reconciliation/components/statement-upload.tsx
  - app/(app)/apps/bank-reconciliation/components/column-mapper.tsx
autonomous: true
requirements: [BANK-01, BANK-02]

must_haves:
  truths:
    - "User can upload a CSV or Excel file via drag-and-drop or file picker"
    - "User can select a bank preset (KBank/SCB/BBL/Other) that pre-populates column mapping"
    - "User sees first 3 preview rows from the uploaded file"
    - "User can change column mapping via dropdown selectors per column"
    - "Import button is disabled until required columns (date, description, deposit or withdrawal) are mapped"
    - "Submitting the import form calls importBankStatementAction and redirects to the review page"
  artifacts:
    - path: "app/(app)/apps/bank-reconciliation/import/page.tsx"
      provides: "Import page with upload form"
      contains: "นำเข้ารายการธนาคาร"
    - path: "app/(app)/apps/bank-reconciliation/components/statement-upload.tsx"
      provides: "File drop zone + bank preset selector"
      contains: "use client"
    - path: "app/(app)/apps/bank-reconciliation/components/column-mapper.tsx"
      provides: "Column mapping table with dropdowns and preview rows"
      contains: "use client"
  key_links:
    - from: "app/(app)/apps/bank-reconciliation/components/statement-upload.tsx"
      to: "services/bank-statement-parser.ts"
      via: "import BANK_PRESETS, parseCSVBuffer, parseExcelBuffer, detectEncoding"
      pattern: "import.*BANK_PRESETS"
    - from: "app/(app)/apps/bank-reconciliation/import/page.tsx"
      to: "app/(app)/apps/bank-reconciliation/actions.ts"
      via: "importBankStatementAction form submission"
      pattern: "importBankStatementAction"
---

<objective>
Build the import flow UI: file upload page with drag-and-drop, bank preset selector, column mapping table with dropdown selectors and 3-row preview, and import submission.

Purpose: Enables users to upload bank statements (CSV/Excel), map columns to expected fields, preview data, and import -- completing BANK-01 (flexible column mapping) and BANK-02 (Thai bank handling) from the user's perspective.
Output: 3 files -- import page, statement upload component, column mapper component.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-bank-reconciliation/07-CONTEXT.md
@.planning/phases/07-bank-reconciliation/07-UI-SPEC.md
@.planning/phases/07-bank-reconciliation/07-02-SUMMARY.md

<interfaces>
<!-- From Plan 01 + 02 outputs -->

From services/bank-statement-parser.ts:
```typescript
export type ColumnMapping = {
  date: number; description: number; deposit: number | null;
  withdrawal: number | null; balance: number | null; reference: number | null;
}
export const BANK_PRESETS: Record<string, {
  label: string; labelTh: string; defaultMapping: ColumnMapping;
  defaultSkipLines: number; encoding: string; dateFormat: string; useBuddhistEra: boolean;
}>
export function detectEncoding(buffer: Buffer): "utf-8" | "windows-874"
export async function parseCSVBuffer(buffer: Buffer, skipLines?: number): Promise<string[][]>
export async function parseExcelBuffer(buffer: Buffer): Promise<string[][]>
```

From app/(app)/apps/bank-reconciliation/actions.ts:
```typescript
export async function importBankStatementAction(
  prevState: ActionState<{ statementId: string }> | null,
  formData: FormData
): Promise<ActionState<{ statementId: string }>>
```

From forms/bank-statement.ts:
```typescript
export const columnMappingSchema: z.ZodObject<...>
export const importBankStatementSchema: z.ZodObject<...>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: StatementUpload component -- file drop zone + bank preset selector</name>
  <files>app/(app)/apps/bank-reconciliation/components/statement-upload.tsx</files>
  <read_first>
    - components/dashboard/drop-zone-widget.tsx (existing drag-and-drop pattern with border-dashed, onDragOver, onDrop)
    - .planning/phases/07-bank-reconciliation/07-UI-SPEC.md (StatementUpload interactions, drop zone specs, bank preset selector)
    - services/bank-statement-parser.ts (BANK_PRESETS import, parseCSVBuffer, parseExcelBuffer for client-side preview parsing)
  </read_first>
  <action>
    Create `app/(app)/apps/bank-reconciliation/components/statement-upload.tsx` as a `"use client"` component.

    **Props:** `onFileLoaded: (file: File, rows: string[][], encoding: string) => void` -- callback when file parsed successfully.

    **State:**
    - `file: File | null` -- selected file
    - `isDragOver: boolean` -- drag-over visual state
    - `bankPreset: string` -- selected bank ("kbank", "scb", "bbl", "other")
    - `previewRows: string[][]` -- parsed rows from file for preview
    - `detectedEncoding: string` -- "utf-8" or "windows-874"
    - `isLoading: boolean` -- while parsing file

    **Drop zone per UI-SPEC:**
    - `border-2 border-dashed rounded-lg p-12` per existing drop-zone pattern
    - Upload icon (lucide `Upload`, 32px, `text-muted-foreground`)
    - Primary text: "ลากไฟล์มาวางที่นี่ หรือ คลิกเพื่อเลือกไฟล์"
    - Sub text: "รองรับไฟล์ CSV และ Excel (.xlsx)" in `text-sm text-muted-foreground`
    - Drag-over state: `border-primary bg-primary/5`, text changes to "วางไฟล์ที่นี่"
    - Hidden `<input type="file" accept=".csv,.xlsx,...">`
    - `role="button"`, `tabIndex={0}`, `aria-label="อัปโหลดไฟล์ CSV หรือ Excel"`, Enter/Space triggers file picker
    - Invalid file type: toast error "รองรับเฉพาะไฟล์ CSV และ Excel (.xlsx)" via sonner

    **Bank preset selector (visible after file loaded) per D-05:**
    - Label: "เลือกธนาคาร"
    - shadcn `Select` with options from BANK_PRESETS + "อื่นๆ" (Other)
    - On selection: set bankPreset state, callback to parent to update column mapping

    **Encoding notice per D-04:**
    - If TIS-620 detected, show Alert (info variant): "ตรวจพบการเข้ารหัส TIS-620 -- แปลงเป็น UTF-8 อัตโนมัติ"

    **File parsing (client-side preview only):**
    - NOTE: The actual file bytes stay in state for FormData submission. The preview parsing here is for column mapper display only.
    - For CSV: read file as ArrayBuffer, detect encoding, decode text, split by newlines for preview rows (first 10 rows including header). The actual import uses server-side parseCSVBuffer.
    - For Excel: similarly extract preview rows from the first sheet.
    - On success: call `onFileLoaded(file, previewRows, encoding)`.

    **CRITICAL:** File parsing for the column mapper preview can be done client-side with simple string splitting for CSV preview. The robust @fast-csv/parse parsing happens server-side in the import action. Keep client-side preview simple.
  </action>
  <verify>
    <automated>cd /Users/ohmmingrath/Projects/banchee && npx tsc --noEmit app/\(app\)/apps/bank-reconciliation/components/statement-upload.tsx 2>&1 | head -20 || echo "Type check complete"</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "use client" app/(app)/apps/bank-reconciliation/components/statement-upload.tsx
    - grep -q "onDragOver" app/(app)/apps/bank-reconciliation/components/statement-upload.tsx
    - grep -q "onDrop" app/(app)/apps/bank-reconciliation/components/statement-upload.tsx
    - grep -q "BANK_PRESETS\|bankPreset\|เลือกธนาคาร" app/(app)/apps/bank-reconciliation/components/statement-upload.tsx
    - grep -q "border-dashed" app/(app)/apps/bank-reconciliation/components/statement-upload.tsx
    - grep -q 'accept=".csv' app/(app)/apps/bank-reconciliation/components/statement-upload.tsx
    - grep -q "อัปโหลดไฟล์" app/(app)/apps/bank-reconciliation/components/statement-upload.tsx
  </acceptance_criteria>
  <done>
    StatementUpload component renders a drag-and-drop zone matching UI-SPEC specs, accepts CSV/Excel files, detects encoding, shows bank preset selector after file load, and displays TIS-620 encoding notice when detected.
  </done>
</task>

<task type="auto">
  <name>Task 2: ColumnMapper component + import page</name>
  <files>app/(app)/apps/bank-reconciliation/components/column-mapper.tsx, app/(app)/apps/bank-reconciliation/import/page.tsx</files>
  <read_first>
    - .planning/phases/07-bank-reconciliation/07-UI-SPEC.md (Column Mapper interactions, import page layout, form field specs)
    - components/ui/select.tsx (shadcn Select usage pattern)
    - components/ui/table.tsx (Table component usage)
    - app/(app)/apps/bank-reconciliation/components/statement-upload.tsx (just created -- props and callbacks)
    - app/(app)/apps/bank-reconciliation/actions.ts (importBankStatementAction for form submission)
  </read_first>
  <action>
    **1. Create column-mapper.tsx** as `"use client"` component per D-02:

    **Props:**
    ```typescript
    {
      previewRows: string[][]     // first N rows from parsed file
      mapping: ColumnMapping       // current column mapping state
      onMappingChange: (mapping: ColumnMapping) => void
    }
    ```

    **Layout per UI-SPEC:**
    - Table with overflow-x-auto wrapper for mobile scroll
    - Header row: one Select dropdown per column from the file
    - Each dropdown has options: วันที่ (Date), รายละเอียด (Description), ฝาก (Deposit), ถอน (Withdrawal), ยอดคงเหลือ (Balance), เลขอ้างอิง (Reference), ข้าม (Skip)
    - Each `aria-label`: "เลือกประเภทข้อมูลสำหรับคอลัมน์ {n}"
    - Cell min-width: 120px, dropdown fills cell (w-full)
    - Preview rows (first 3 data rows): `bg-muted/50 rounded text-sm font-medium text-muted-foreground`
    - If file has < 3 rows, show all available
    - Skipped columns: preview cells show `text-muted-foreground/50` (dimmed)

    **Exclusive selection per UI-SPEC:**
    - Each field (Date, Description, etc.) can only be assigned to ONE column
    - Selecting it elsewhere auto-sets the previous column to "Skip"
    - "Skip" can be on multiple columns

    **Validation per UI-SPEC:**
    - Required: Date AND Description AND (Deposit OR Withdrawal)
    - If missing: show inline message below table: "กรุณาเลือกคอลัมน์สำหรับ วันที่, รายละเอียด, และ ฝาก หรือ ถอน อย่างน้อย 1 คอลัมน์"

    **2. Create import/page.tsx** as Server Component page:

    **Layout per UI-SPEC:**
    - Page heading: "นำเข้ารายการธนาคาร" (`text-3xl font-bold tracking-tight`)
    - Back link: "< กลับรายการ" linking to `/apps/bank-reconciliation` (`text-sm text-muted-foreground`)
    - Render `ImportForm` client component that orchestrates StatementUpload + ColumnMapper + submit button

    **ImportForm (inline client component or separate):**
    - State: file, previewRows, bankPreset, columnMapping, encoding
    - StatementUpload passes file + preview rows up
    - ColumnMapper shows after file loaded, with mapping pre-set from bank preset per D-05
    - Bank preset change re-applies default mapping to ColumnMapper
    - Submit button: "นำเข้าและจับคู่อัตโนมัติ" (`size="lg"`, `w-full sm:w-auto`)
    - Loading state: Loader2 spinner + "กำลังนำเข้า..."
    - Button disabled when: no file, required columns not mapped, or loading
    - On submit: create FormData with file + bankName + column mapping indices + skipLines, call `importBankStatementAction` via `useActionState`
    - On success: redirect to `/apps/bank-reconciliation/${data.statementId}` via `router.push`
    - On error: show FormError with server error message

    `export const dynamic = "force-dynamic"` on the page.
  </action>
  <verify>
    <automated>cd /Users/ohmmingrath/Projects/banchee && npx tsc --noEmit app/\(app\)/apps/bank-reconciliation/components/column-mapper.tsx app/\(app\)/apps/bank-reconciliation/import/page.tsx 2>&1 | head -20 || echo "Type check complete"</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "use client" app/(app)/apps/bank-reconciliation/components/column-mapper.tsx
    - grep -q "onMappingChange" app/(app)/apps/bank-reconciliation/components/column-mapper.tsx
    - grep -q "วันที่" app/(app)/apps/bank-reconciliation/components/column-mapper.tsx
    - grep -q "รายละเอียด" app/(app)/apps/bank-reconciliation/components/column-mapper.tsx
    - grep -q "ฝาก" app/(app)/apps/bank-reconciliation/components/column-mapper.tsx
    - grep -q "ถอน" app/(app)/apps/bank-reconciliation/components/column-mapper.tsx
    - grep -q "นำเข้ารายการธนาคาร" app/(app)/apps/bank-reconciliation/import/page.tsx
    - grep -q "importBankStatementAction" app/(app)/apps/bank-reconciliation/import/page.tsx
    - grep -q "กลับรายการ" app/(app)/apps/bank-reconciliation/import/page.tsx
    - grep -q "นำเข้าและจับคู่อัตโนมัติ" app/(app)/apps/bank-reconciliation/import/page.tsx
  </acceptance_criteria>
  <done>
    Column mapper renders dropdown selectors per column with exclusive selection logic and 3-row preview. Import page orchestrates the full import flow: upload -> select bank -> map columns -> submit. Form submission calls importBankStatementAction and redirects to review page on success.
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes on all new components
- Import page renders with heading, back link, upload section, and import button
- Column mapper shows dropdown per column with all 7 options
- Drop zone accepts CSV and Excel files
- Bank preset pre-selects column mapping
</verification>

<success_criteria>
1. File upload accepts CSV and Excel via drag-drop or click per D-01
2. Bank preset selector offers KBank, SCB, BBL, Other per D-05
3. Column mapping dropdowns show all 7 field options per D-02
4. Preview shows first 3 data rows below column headers
5. Exclusive column selection: each field assigned to only one column
6. Import button disabled until required columns mapped
7. Form submits to importBankStatementAction and redirects on success
8. TIS-620 encoding notice displays when detected per D-04
</success_criteria>

<output>
After completion, create `.planning/phases/07-bank-reconciliation/07-03-SUMMARY.md`
</output>
