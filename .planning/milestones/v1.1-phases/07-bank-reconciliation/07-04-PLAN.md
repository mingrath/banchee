---
phase: 07-bank-reconciliation
plan: 04
type: execute
wave: 3
depends_on: ["07-02"]
files_modified:
  - app/(app)/apps/bank-reconciliation/page.tsx
  - app/(app)/apps/bank-reconciliation/[statementId]/page.tsx
  - app/(app)/apps/bank-reconciliation/components/statement-list.tsx
  - app/(app)/apps/bank-reconciliation/components/match-review-table.tsx
  - app/(app)/apps/bank-reconciliation/components/reconciliation-progress.tsx
autonomous: false
requirements: [BANK-03, BANK-04, BANK-05, BANK-06]

must_haves:
  truths:
    - "User can see a list of imported statements with bank name, filename, date, progress, and status badge"
    - "User can click a statement row to navigate to its review page"
    - "User can see bank entries paired with matched BanChee transactions in a two-column layout"
    - "User can confirm or reject each suggested match"
    - "User can create a new transaction from an unmatched bank entry with one click"
    - "User can skip an unmatched entry"
    - "User can undo a confirmed match"
    - "Progress bar updates after each action and shows X/Y entries resolved"
    - "Statement status auto-updates to reconciled when all entries are resolved"
    - "Deposit amounts display in green, withdrawal amounts in red"
  artifacts:
    - path: "app/(app)/apps/bank-reconciliation/page.tsx"
      provides: "Statement list page (Server Component)"
      contains: "กระทบยอดธนาคาร"
    - path: "app/(app)/apps/bank-reconciliation/[statementId]/page.tsx"
      provides: "Review page for a single statement"
      contains: "กระทบยอด"
    - path: "app/(app)/apps/bank-reconciliation/components/statement-list.tsx"
      provides: "Table of imported statements with status and progress"
      contains: "use client"
    - path: "app/(app)/apps/bank-reconciliation/components/match-review-table.tsx"
      provides: "Two-column review table with confirm/reject/create/skip actions"
      contains: "use client"
    - path: "app/(app)/apps/bank-reconciliation/components/reconciliation-progress.tsx"
      provides: "Progress bar with resolved/total label"
      contains: "use client"
  key_links:
    - from: "app/(app)/apps/bank-reconciliation/page.tsx"
      to: "models/bank-statements.ts"
      via: "getBankStatements query"
      pattern: "import.*getBankStatements"
    - from: "app/(app)/apps/bank-reconciliation/[statementId]/page.tsx"
      to: "models/bank-statements.ts"
      via: "getBankStatementById query"
      pattern: "import.*getBankStatementById"
    - from: "app/(app)/apps/bank-reconciliation/components/match-review-table.tsx"
      to: "app/(app)/apps/bank-reconciliation/actions.ts"
      via: "confirmMatchAction, rejectMatchAction, etc."
      pattern: "import.*confirmMatchAction"
---

<objective>
Build the reconciliation review UI and statement list page: statement list with status badges and progress indicators, match review table with confirm/reject/create/skip actions, reconciliation progress bar, and visual verification of the full flow.

Purpose: Enables users to review matched pairs, take action on each entry, track reconciliation progress, and manage imported statements -- completing BANK-03 (auto-matching display), BANK-04 (review UI), BANK-05 (create from unmatched), and BANK-06 (status tracking) from the user's perspective.
Output: 5 files -- 2 pages (list + review) and 3 client components.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-bank-reconciliation/07-CONTEXT.md
@.planning/phases/07-bank-reconciliation/07-UI-SPEC.md
@.planning/phases/07-bank-reconciliation/07-02-SUMMARY.md

<interfaces>
<!-- From Plan 02 outputs -->

From models/bank-statements.ts:
```typescript
export const getBankStatements = cache(async (userId: string) => BankStatement[])
export const getBankStatementById = cache(async (userId: string, statementId: string) => BankStatement & { entries: BankEntry[] })
export async function getResolvedEntryCount(statementId: string): Promise<{ resolved: number; total: number }>
```

From app/(app)/apps/bank-reconciliation/actions.ts:
```typescript
export async function confirmMatchAction(entryId: string, transactionId: string): Promise<ActionState<void>>
export async function rejectMatchAction(entryId: string): Promise<ActionState<void>>
export async function createTransactionFromEntryAction(entryId: string): Promise<ActionState<void>>
export async function skipEntryAction(entryId: string): Promise<ActionState<void>>
export async function undoMatchAction(entryId: string): Promise<ActionState<void>>
export async function deleteStatementAction(statementId: string): Promise<ActionState<void>>
```

From Prisma types (via prisma/client):
```typescript
type BankStatement = {
  id: string; bankName: string; filename: string; status: string;
  totalEntries: number; matchedEntries: number; createdAt: Date;
}
type BankEntry = {
  id: string; date: Date; description: string; deposit: number | null;
  withdrawal: number | null; balance: number | null; reference: string | null;
  matchStatus: string; transactionId: string | null;
  matchScore: number | null; matchReasons: unknown;
}
```

From existing components:
```typescript
// app/(app)/apps/documents/components/status-badge.tsx
export function StatusBadge({ status, statusMap }: { status: string; statusMap?: Record<string, { label: string; className: string }> })

// components/ui/progress.tsx -- shadcn Progress component
// components/ui/collapsible.tsx -- shadcn Collapsible for alternatives
// components/ui/dialog.tsx -- Dialog for delete confirmation
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Statement list page + StatementList + ReconciliationProgress components</name>
  <files>app/(app)/apps/bank-reconciliation/page.tsx, app/(app)/apps/bank-reconciliation/components/statement-list.tsx, app/(app)/apps/bank-reconciliation/components/reconciliation-progress.tsx</files>
  <read_first>
    - app/(app)/apps/documents/page.tsx (existing document list page pattern -- Server Component, getCurrentUser, model query, render client component)
    - app/(app)/apps/documents/components/document-table.tsx (table component pattern with status badges)
    - app/(app)/apps/documents/components/status-badge.tsx (StatusBadge with custom statusMap)
    - .planning/phases/07-bank-reconciliation/07-UI-SPEC.md (statement list layout, reconciliation status badge colors, match status badge colors, progress bar, loading states, empty state)
    - components/ui/progress.tsx (shadcn Progress component)
    - models/bank-statements.ts (getBankStatements, getResolvedEntryCount)
  </read_first>
  <action>
    **1. Create reconciliation-progress.tsx** as `"use client"` component:
    - Props: `resolved: number, total: number`
    - Layout per UI-SPEC: `flex items-center gap-4`
    - Shadcn `Progress` component: `className="flex-1 h-2"`, value = `(resolved / total) * 100`
    - Label: `"{resolved}/{total} รายการ"` in `text-sm font-medium`

    **2. Create statement-list.tsx** as `"use client"` component per D-15:
    - Props: `statements: BankStatement[]` (with progress data injected by parent page)
    - Table per UI-SPEC layout:
      - Headers: ธนาคาร | ชื่อไฟล์ | วันที่นำเข้า | รายการ | ความคืบหน้า | สถานะ
      - Each row: cursor-pointer, `hover:bg-muted/50`, onClick navigates to `/apps/bank-reconciliation/${statement.id}`
      - Bank name: show Thai label from BANK_PRESETS (e.g., "กสิกรไทย" for "kbank")
      - Date: format with Thai locale (day/month/B.E. year)
      - Entry count: `"{totalEntries} รายการ"`
      - Progress column: inline `Progress` (h-2) + `"{matchedEntries}/{totalEntries}"` text
      - Status column: `StatusBadge` with reconciliation status map:
        - imported: `bg-blue-100 text-blue-700` / dark variants, label "นำเข้าแล้ว"
        - in_progress: `bg-orange-100 text-orange-700` / dark variants, label "กำลังดำเนินการ"
        - reconciled: `bg-green-100 text-green-700` / dark variants, label "กระทบยอดเสร็จ"
    - Delete: trailing Trash2 icon button (variant="ghost", size="icon"). Opens Dialog confirmation per UI-SPEC copywriting. Calls `deleteStatementAction`.
    - Empty state per UI-SPEC: centered in Card, heading "ยังไม่มีรายการธนาคาร", body "นำเข้ารายการจากไฟล์ CSV หรือ Excel เพื่อเริ่มกระทบยอด", CTA button "นำเข้ารายการ" linking to import page.
    - Loading state: 3 Skeleton rows per UI-SPEC.

    **3. Create page.tsx** as Server Component:
    - `export const dynamic = "force-dynamic"`
    - Call `getCurrentUser()` for auth
    - Fetch statements via `getBankStatements(user.id)`
    - Layout per UI-SPEC:
      - Page heading: "กระทบยอดธนาคาร" (`text-3xl font-bold tracking-tight`)
      - Action row: `flex items-center justify-between mb-6`
        - Left: description "นำเข้ารายการจาก CSV หรือ Excel แล้วจับคู่กับรายการใน BanChee" (`text-sm text-muted-foreground`)
        - Right: Button "นำเข้ารายการใหม่" linking to `/apps/bank-reconciliation/import` (Upload icon, variant="default")
    - Render `StatementList` with fetched data
  </action>
  <verify>
    <automated>cd /Users/ohmmingrath/Projects/banchee && npx tsc --noEmit app/\(app\)/apps/bank-reconciliation/page.tsx app/\(app\)/apps/bank-reconciliation/components/statement-list.tsx app/\(app\)/apps/bank-reconciliation/components/reconciliation-progress.tsx 2>&1 | head -20 || echo "Type check complete"</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "กระทบยอดธนาคาร" app/(app)/apps/bank-reconciliation/page.tsx
    - grep -q "getBankStatements" app/(app)/apps/bank-reconciliation/page.tsx
    - grep -q "getCurrentUser" app/(app)/apps/bank-reconciliation/page.tsx
    - grep -q "นำเข้ารายการใหม่" app/(app)/apps/bank-reconciliation/page.tsx
    - grep -q "force-dynamic" app/(app)/apps/bank-reconciliation/page.tsx
    - grep -q "use client" app/(app)/apps/bank-reconciliation/components/statement-list.tsx
    - grep -q "ธนาคาร" app/(app)/apps/bank-reconciliation/components/statement-list.tsx
    - grep -q "deleteStatementAction" app/(app)/apps/bank-reconciliation/components/statement-list.tsx
    - grep -q "ยังไม่มีรายการธนาคาร" app/(app)/apps/bank-reconciliation/components/statement-list.tsx
    - grep -q "use client" app/(app)/apps/bank-reconciliation/components/reconciliation-progress.tsx
    - grep -q "Progress" app/(app)/apps/bank-reconciliation/components/reconciliation-progress.tsx
    - grep -q "รายการ" app/(app)/apps/bank-reconciliation/components/reconciliation-progress.tsx
  </acceptance_criteria>
  <done>
    Statement list page shows all imported statements with bank name, filename, date, progress bar, and status badges. Empty state renders import CTA. ReconciliationProgress shows X/Y progress bar. Navigation to review page works via row click.
  </done>
</task>

<task type="auto">
  <name>Task 2: MatchReviewTable component + review page</name>
  <files>app/(app)/apps/bank-reconciliation/components/match-review-table.tsx, app/(app)/apps/bank-reconciliation/[statementId]/page.tsx</files>
  <read_first>
    - .planning/phases/07-bank-reconciliation/07-UI-SPEC.md (review page layout, MatchReviewTable interactions, match status badge colors, amount coloring, alternatives expand, loading states, responsive behavior, accessibility)
    - app/(app)/apps/bank-reconciliation/actions.ts (all match actions -- confirm, reject, create, skip, undo)
    - models/bank-statements.ts (getBankStatementById, getResolvedEntryCount)
    - services/tax-calculator.ts (formatCurrency for amount display)
    - components/ui/collapsible.tsx (Collapsible pattern for alternatives)
    - components/ui/tooltip.tsx (Tooltip for icon buttons)
    - app/(app)/apps/bank-reconciliation/components/reconciliation-progress.tsx (just created)
  </read_first>
  <action>
    **1. Create match-review-table.tsx** as `"use client"` component per D-08, D-09, D-10:

    **Props:**
    ```typescript
    {
      entries: BankEntry[]  // with related transaction data for matched entries
      onActionComplete?: () => void  // optional callback to refresh data
    }
    ```

    **Layout per UI-SPEC -- two-column table:**
    - Table with `rounded-md border`
    - Headers: "รายการธนาคาร (Bank Entry)" | "รายการ BanChee (Matched Tx)"
    - Each row renders a bank entry on the left, matched transaction (if any) on the right

    **Left column (bank entry):**
    - Date (Thai format, short month)
    - Description
    - Amount: deposit in green (`text-green-700 dark:text-green-400`) with "+" prefix, withdrawal in red (`text-red-700 dark:text-red-400`) with "-" prefix per D-03 and UI-SPEC
    - Match score badge (if suggested): color by confidence level per UI-SPEC (>=80% green, 60-79% yellow, <60% gray)
    - Match status badge using match status color map from UI-SPEC

    **Right column (matched transaction or actions):**
    - If suggested match: show transaction name, date, amount. Action buttons: Confirm (Check icon) + Reject (X icon). Each wrapped in Tooltip per UI-SPEC accessibility.
    - If no match: text "ไม่พบรายการที่ตรงกัน". Action buttons: "สร้างรายการ" (Plus icon) + "ข้าม" (SkipForward icon, variant="outline").
    - If confirmed: show matched transaction info + "ยืนยันแล้ว" badge (green). Undo button (Undo2 icon, variant="ghost") per UI-SPEC.
    - If created: show "สร้างรายการแล้ว" badge (purple). No actions.
    - If skipped: show "ข้าม" badge (muted). No actions.

    **Alternatives (D-09):**
    - If entry has multiple match candidates (matchReasons is array with >1), show `Collapsible`:
      - Trigger: "ดูตัวเลือกอื่น" with ChevronDown icon
      - Content: list of alternative candidates with name, score, "สลับ" (Switch) button
      - Switch calls manual match: `confirmMatchAction(entryId, alternativeTransactionId)` and resets matchStatus to "suggested" first

    **Row visual treatment per UI-SPEC:**
    - Unresolved: default background
    - Confirmed/created: `bg-green-50/30 dark:bg-green-900/10` with reduced opacity
    - Skipped: `bg-muted/30 text-muted-foreground`

    **Action buttons per UI-SPEC:**
    - Use `useTransition` per action to isolate loading states
    - Only clicked button shows Loader2 spinner, others remain enabled
    - Each action calls the corresponding server action, then triggers `onActionComplete`

    **Responsive per UI-SPEC:**
    - Mobile (<sm): stack bank entry on top, match info below, action buttons below match within each row
    - Desktop (>=sm): two-column layout (bank entry left, matched transaction right)

    **2. Create [statementId]/page.tsx** as Server Component:
    - `export const dynamic = "force-dynamic"`
    - Params: `{ statementId: string }`
    - Call `getCurrentUser()`, then `getBankStatementById(user.id, statementId)`
    - If not found: redirect or notFound()
    - Fetch resolved count via `getResolvedEntryCount(statementId)`
    - Also need to fetch the matched Transaction details for each entry that has a transactionId -- do this via a model query that joins entries with their matched transactions.

    **Layout per UI-SPEC:**
    - Header: `flex items-center justify-between`
      - Left: "กระทบยอด: {filename}" (`text-2xl font-bold`) + `StatusBadge`
      - Right: Button "กลับรายการ" (`variant="outline"`, ArrowLeft icon) linking to `/apps/bank-reconciliation`
    - `ReconciliationProgress` (`mb-6`) with resolved/total counts
    - `MatchReviewTable` with entries data
    - When 100% resolved (all entries confirmed/created/skipped): toast "กระทบยอดเสร็จสิ้น" via client component effect

    **CRITICAL per Pitfall 3:** Display amounts using satang values from BankEntry directly. Use `formatCurrency` or manual `(satang / 100).toLocaleString()` -- never double-divide. Bank entries store deposit/withdrawal in satang already.
  </action>
  <verify>
    <automated>cd /Users/ohmmingrath/Projects/banchee && npx tsc --noEmit app/\(app\)/apps/bank-reconciliation/components/match-review-table.tsx app/\(app\)/apps/bank-reconciliation/\[statementId\]/page.tsx 2>&1 | head -20 || echo "Type check complete"</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "use client" app/(app)/apps/bank-reconciliation/components/match-review-table.tsx
    - grep -q "confirmMatchAction" app/(app)/apps/bank-reconciliation/components/match-review-table.tsx
    - grep -q "rejectMatchAction" app/(app)/apps/bank-reconciliation/components/match-review-table.tsx
    - grep -q "createTransactionFromEntryAction" app/(app)/apps/bank-reconciliation/components/match-review-table.tsx
    - grep -q "skipEntryAction" app/(app)/apps/bank-reconciliation/components/match-review-table.tsx
    - grep -q "undoMatchAction" app/(app)/apps/bank-reconciliation/components/match-review-table.tsx
    - grep -q "text-green" app/(app)/apps/bank-reconciliation/components/match-review-table.tsx
    - grep -q "text-red" app/(app)/apps/bank-reconciliation/components/match-review-table.tsx
    - grep -q "ดูตัวเลือกอื่น" app/(app)/apps/bank-reconciliation/components/match-review-table.tsx
    - grep -q "สร้างรายการ" app/(app)/apps/bank-reconciliation/components/match-review-table.tsx
    - grep -q "กระทบยอด" app/(app)/apps/bank-reconciliation/[statementId]/page.tsx
    - grep -q "getBankStatementById" app/(app)/apps/bank-reconciliation/[statementId]/page.tsx
    - grep -q "ReconciliationProgress" app/(app)/apps/bank-reconciliation/[statementId]/page.tsx
    - grep -q "MatchReviewTable" app/(app)/apps/bank-reconciliation/[statementId]/page.tsx
  </acceptance_criteria>
  <done>
    Match review table renders two-column layout with bank entry left and matched transaction right. All 5 actions (confirm/reject/create/skip/undo) work with isolated loading states. Deposits display green, withdrawals red. Alternatives expand via Collapsible. Review page shows progress bar and auto-detects reconciliation completion. Statement list page navigates to review page.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of full bank reconciliation flow</name>
  <files>none</files>
  <action>
    Human visual verification of the complete bank reconciliation feature built in Plans 01-04. No code changes -- just verification of the end-to-end flow.
  </action>
  <what-built>
    Complete bank reconciliation feature: statement import with CSV/Excel upload, bank preset selection, column mapping, auto-matching, review table with confirm/reject/create/skip/undo actions, progress tracking, and statement list with status badges.
  </what-built>
  <how-to-verify>
    1. Navigate to http://localhost:7331/apps/bank-reconciliation
    2. Verify empty state shows "ยังไม่มีรายการธนาคาร" with import button
    3. Click "นำเข้ารายการใหม่" -- verify import page loads
    4. Create a test CSV file with Thai bank format (date, description, withdrawal, deposit, balance columns) and upload it via drag-and-drop or click
    5. Verify bank preset selector appears, select a bank
    6. Verify column mapping table shows 3 preview rows with dropdown per column
    7. Change column assignments and verify exclusive selection (one field per column)
    8. Click "นำเข้าและจับคู่อัตโนมัติ" -- verify redirect to review page
    9. On review page: verify progress bar shows "X/Y รายการ"
    10. For matched entries: verify green deposit / red withdrawal amounts, match score badge
    11. Click confirm on a match -- verify row turns green, progress updates
    12. Click reject on a match -- verify row reverts to unmatched with create/skip buttons
    13. Click "สร้างรายการ" on unmatched entry -- verify purple badge appears
    14. Click "ข้าม" on an entry -- verify gray badge appears
    15. Go back to statement list -- verify progress and status badge are correct
  </how-to-verify>
  <verify>Human confirms all 15 verification steps pass visually</verify>
  <done>User approves the full bank reconciliation flow end-to-end</done>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- Statement list page loads at /apps/bank-reconciliation with correct layout
- Import page loads at /apps/bank-reconciliation/import
- Review page loads at /apps/bank-reconciliation/[id]
- All action buttons work (confirm, reject, create, skip, undo)
- Progress bar updates correctly after each action
- Statement status transitions through imported -> in_progress -> reconciled
- Deposit/withdrawal amounts colored correctly (green/red)
- StatusBadge displays correct reconciliation status colors
</verification>

<success_criteria>
1. Statement list shows all imported statements with progress and status per D-15
2. Each row navigates to review page on click
3. Two-column review table shows bank entries vs matched transactions per D-08
4. Confirm/reject/create/skip/undo actions work per D-08, D-09, D-10
5. Deposit amounts green, withdrawal amounts red
6. Ambiguous matches show alternatives via Collapsible per D-09
7. Progress bar accurately tracks resolved entries
8. Status auto-updates to "reconciled" when 100% resolved per D-14
9. Visual verification confirms full flow works end-to-end
</success_criteria>

<output>
After completion, create `.planning/phases/07-bank-reconciliation/07-04-SUMMARY.md`
</output>
