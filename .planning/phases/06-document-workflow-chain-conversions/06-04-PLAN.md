---
phase: 06-document-workflow-chain-conversions
plan: 04
type: execute
wave: 4
depends_on: ["06-03"]
files_modified:
  - app/(app)/apps/documents/manifest.ts
  - app/(app)/apps/documents/page.tsx
  - app/(app)/apps/documents/components/document-table.tsx
  - app/(app)/apps/documents/components/chain-badges.tsx
  - app/(app)/apps/documents/components/document-filters.tsx
  - app/(app)/apps/documents/components/status-badge.tsx
  - app/(app)/apps/quotation/[id]/detail-actions.tsx
  - app/(app)/apps/invoice/[id]/detail-actions.tsx
  - app/(app)/apps/invoice/components/payment-progress-bar.tsx
  - app/(app)/apps/invoice/[id]/page.tsx
autonomous: true
requirements: [DOC-08, QUOT-06]
must_haves:
  truths:
    - "User can view all documents (quotations, invoices, receipts, delivery notes) in a unified list at /apps/documents per D-19"
    - "Each row shows inline chain badges (QT -> INV -> RCT) as clickable links per D-20"
    - "Filters work: document type dropdown, status dropdown, date range per D-21"
    - "Quotation-only list at /apps/quotation remains unchanged per D-22"
    - "Quotation detail page has conversion buttons: 'สร้างใบแจ้งหนี้' when accepted, 'สร้างใบส่งของ' when accepted/converted"
    - "Invoice detail page has payment progress bar showing partial payment status"
  artifacts:
    - path: "app/(app)/apps/documents/page.tsx"
      provides: "Unified document list server component"
      contains: "listDocumentsWithChain"
    - path: "app/(app)/apps/documents/components/document-table.tsx"
      provides: "Table with all document types, type column, status column, chain column"
      exports: ["DocumentTable"]
    - path: "app/(app)/apps/documents/components/chain-badges.tsx"
      provides: "Clickable chain badges showing linked documents"
      exports: ["ChainBadges"]
    - path: "app/(app)/apps/documents/components/document-filters.tsx"
      provides: "Type, status, date range filter row"
      exports: ["DocumentFilters"]
    - path: "app/(app)/apps/documents/components/status-badge.tsx"
      provides: "Generic StatusBadge accepting any status map"
      exports: ["StatusBadge"]
    - path: "app/(app)/apps/invoice/components/payment-progress-bar.tsx"
      provides: "Progress bar with paid/remaining labels"
      exports: ["PaymentProgressBar"]
  key_links:
    - from: "app/(app)/apps/documents/page.tsx"
      to: "models/documents.ts"
      via: "listDocumentsWithChain for unified list with source/derived includes"
      pattern: "listDocumentsWithChain"
    - from: "app/(app)/apps/documents/components/chain-badges.tsx"
      to: "app/(app)/apps/invoice/[id]/page.tsx"
      via: "Link href to each document's detail page"
      pattern: "Link.*href.*apps"
    - from: "app/(app)/apps/quotation/[id]/detail-actions.tsx"
      to: "app/(app)/apps/invoice/actions.ts"
      via: "convertQuotationToInvoiceAction formAction"
      pattern: "convertQuotationToInvoiceAction"
---

<objective>
Build the unified document list page with chain badges and filters, add conversion buttons to the quotation detail page, and add the payment progress bar to the invoice detail page. This ties all document types together into a coherent workflow.

Purpose: The unified list (DOC-08) is how users see the full document chain. The quotation conversion buttons (QUOT-06) complete the quotation-to-invoice flow. The payment progress bar shows partial payment status.
Output: Working /apps/documents page, updated quotation detail with conversion buttons, invoice detail with payment progress.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-document-workflow-chain-conversions/06-CONTEXT.md
@.planning/phases/06-document-workflow-chain-conversions/06-RESEARCH.md
@.planning/phases/06-document-workflow-chain-conversions/06-UI-SPEC.md
@.planning/phases/06-document-workflow-chain-conversions/06-01-SUMMARY.md
@.planning/phases/06-document-workflow-chain-conversions/06-02-SUMMARY.md
@.planning/phases/06-document-workflow-chain-conversions/06-03-SUMMARY.md

<interfaces>
<!-- Existing components to modify -->
From app/(app)/apps/quotation/[id]/detail-actions.tsx:
```typescript
// Currently has: PDF download, status transition buttons (send, accept, reject, void)
// MUST ADD: conversion buttons for "สร้างใบแจ้งหนี้" and "สร้างใบส่งของ"
export function QuotationDetailActions({
  documentId, currentStatus, effectiveStatus, documentNumber, quotationData
})
```

From app/(app)/apps/invoice/[id]/page.tsx:
```typescript
// Currently has: header, document summary, line items, payment status card (placeholder), related docs
// MUST ADD: PaymentProgressBar component in payment status card
```

<!-- New data sources -->
From models/documents.ts (Plan 01):
```typescript
export async function listDocumentsWithChain(userId: string, filters?: {
  documentType?: string; status?: string;
  dateFrom?: Date; dateTo?: Date; contactId?: string;
  limit?: number; offset?: number;
})
// Returns documents with:
//   sourceDocument: { id, documentNumber, documentType } | null
//   derivedDocuments: { id, documentNumber, documentType }[]
```

From services/document-workflow.ts (Plan 01):
```typescript
export const QUOTATION_STATUSES = { draft, sent, accepted, rejected, expired, converted, voided }
export const INVOICE_STATUSES = { draft, sent, overdue, paid, voided }
export const RECEIPT_STATUSES = { draft, confirmed, voided }
export const DELIVERY_NOTE_STATUSES = { draft, delivered, voided }
export const ALL_DOCUMENT_STATUSES = { ...all merged }
```

<!-- UI spec values -->
Document type badge colors (from 06-UI-SPEC):
  QUOTATION: bg-slate-100 dark:bg-slate-800 text-slate-700 dark:text-slate-300
  INVOICE: bg-blue-100 dark:bg-blue-800 text-blue-700 dark:text-blue-300
  RECEIPT: bg-green-100 dark:bg-green-800 text-green-700 dark:text-green-300
  DELIVERY_NOTE: bg-amber-100 dark:bg-amber-800 text-amber-700 dark:text-amber-300
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Unified document list page with chain badges and filters</name>
  <files>
    app/(app)/apps/documents/manifest.ts,
    app/(app)/apps/documents/page.tsx,
    app/(app)/apps/documents/components/document-table.tsx,
    app/(app)/apps/documents/components/chain-badges.tsx,
    app/(app)/apps/documents/components/document-filters.tsx,
    app/(app)/apps/documents/components/status-badge.tsx
  </files>
  <read_first>
    app/(app)/apps/quotation/page.tsx,
    app/(app)/apps/quotation/components/quotation-list.tsx,
    app/(app)/apps/quotation/components/status-badge.tsx,
    models/documents.ts,
    services/document-workflow.ts,
    .planning/phases/06-document-workflow-chain-conversions/06-UI-SPEC.md
  </read_first>
  <action>
    1. Create manifest.ts: `{ code: "documents", name: "เอกสารทั้งหมด", description: "ดูเอกสารทุกประเภทในที่เดียว", icon: "Files" }`

    2. Create status-badge.tsx (GENERIC version per Pitfall 4):
    - Accept props: `{ status: string, statusMap: Record<string, { label: string; color: string }> }`
    - Use ALL_DOCUMENT_STATUSES as default statusMap
    - Status style mapping (same as quotation StatusBadge but dynamic):
      - secondary -> "bg-secondary text-secondary-foreground"
      - blue -> "bg-blue-100 dark:bg-blue-900/30 text-blue-700 dark:text-blue-300"
      - green -> "bg-green-100 dark:bg-green-900/30 text-green-700 dark:text-green-300"
      - destructive -> "bg-destructive/10 text-destructive"
      - orange -> "bg-orange-100 dark:bg-orange-900/30 text-orange-700 dark:text-orange-300"
      - purple -> "bg-purple-100 dark:bg-purple-900/30 text-purple-700 dark:text-purple-300"
      - muted -> "bg-muted text-muted-foreground"
    - Uses Badge component from @/components/ui/badge

    3. Create chain-badges.tsx (per D-20, UI spec Chain Badges):
    - "use client" component
    - Props: sourceDocument?: { id, documentNumber, documentType } | null, derivedDocuments: { id, documentNumber, documentType }[], currentDoc: { id, documentNumber, documentType }
    - Build chain array: [sourceDocument, currentDoc, ...derivedDocuments] (only non-null entries)
    - Render each as Badge wrapped in Link:
      - href: /apps/{type-to-route-map}/{id} where QUOTATION->"quotation", INVOICE->"invoice", RECEIPT->"receipt", DELIVERY_NOTE->"delivery-note"
      - Badge text: abbreviated doc number (e.g., "QT-0001" from "QT-2568-0001" -- show prefix + last 4 digits)
      - Badge colors per document type from UI spec badge color table
      - ArrowRight icon (h-3 w-3 text-muted-foreground) between badges
      - hover:opacity-80 transition, cursor-pointer
      - text-xs, px-2 py-0.5

    4. Create document-filters.tsx (per D-21, UI spec):
    - "use client" component
    - Props: onFilterChange callback, currentFilters state
    - Layout: flex flex-wrap items-center gap-3 mb-4
    - Type dropdown (Select, w-[180px]):
      - ทั้งหมด (default), ใบเสนอราคา, ใบแจ้งหนี้, ใบเสร็จรับเงิน, ใบส่งของ
    - Status dropdown (Select, w-[160px]):
      - ทั้งหมด (default), then statuses filtered by selected type
      - If type "ทั้งหมด": show union of all statuses (use ALL_DOCUMENT_STATUSES keys)
      - If specific type: show only that type's statuses
    - Date from/to: Popover + Calendar components (w-[140px] each)
    - Client-side filtering (per Claude's discretion and research recommendation for small datasets)

    5. Create document-table.tsx (per D-19, UI spec Unified Document List layout):
    - "use client" component
    - Props: documents array (with sourceDocument + derivedDocuments from listDocumentsWithChain), filters state
    - Client-side filter application: filter by type, status, date range
    - Apply lazy overdue/expired detection for display status
    - Table columns per UI spec: เลขที่ | ประเภท | วันที่ | ลูกค้า | ยอดรวม | สถานะ | เอกสารที่เกี่ยวข้อง
    - Type column: document type badge with type-specific color (not status badge)
    - Status column: StatusBadge with ALL_DOCUMENT_STATUSES
    - Chain column: ChainBadges component
    - Row click navigates to document detail page
    - Empty state: "ยังไม่มีเอกสาร" / "สร้างเอกสารใหม่จากหน้าใบเสนอราคาหรือใบแจ้งหนี้"

    6. Create page.tsx (server component, per D-19):
    - export const dynamic = "force-dynamic"
    - getCurrentUser + listDocumentsWithChain (no type filter = all types)
    - Page heading: "เอกสารทั้งหมด" (text-3xl font-bold tracking-tight)
    - Render DocumentFilters + DocumentTable
    - Pass documents with sourceDocument/derivedDocuments for chain display
  </action>
  <verify>
    <automated>cd /Users/ohmmingrath/Projects/banchee && npx next build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep "listDocumentsWithChain" app/(app)/apps/documents/page.tsx returns matches
    - grep "ChainBadges" app/(app)/apps/documents/components/document-table.tsx returns matches
    - grep "StatusBadge" app/(app)/apps/documents/components/document-table.tsx returns matches
    - grep "ArrowRight" app/(app)/apps/documents/components/chain-badges.tsx returns matches
    - grep "ALL_DOCUMENT_STATUSES" app/(app)/apps/documents/components/status-badge.tsx returns matches
    - grep "DocumentFilters" app/(app)/apps/documents/components/document-filters.tsx returns matches
    - All files exist and TypeScript compiles
  </acceptance_criteria>
  <done>
    Unified document list at /apps/documents shows all document types in one table.
    Chain badges show linked documents (QT -> INV -> RCT) as clickable links.
    Filters work: document type, status (filtered by type), date range.
    Generic StatusBadge works with any document type's status map.
    Empty state renders correctly when no documents match filters.
  </done>
</task>

<task type="auto">
  <name>Task 2: Quotation conversion buttons + invoice payment progress bar</name>
  <files>
    app/(app)/apps/quotation/[id]/detail-actions.tsx,
    app/(app)/apps/invoice/components/payment-progress-bar.tsx,
    app/(app)/apps/invoice/[id]/page.tsx,
    app/(app)/apps/invoice/[id]/detail-actions.tsx
  </files>
  <read_first>
    app/(app)/apps/quotation/[id]/detail-actions.tsx,
    app/(app)/apps/invoice/[id]/detail-actions.tsx,
    app/(app)/apps/invoice/[id]/page.tsx,
    app/(app)/apps/invoice/actions.ts,
    app/(app)/apps/receipt/actions.ts,
    app/(app)/apps/delivery-note/actions.ts,
    .planning/phases/06-document-workflow-chain-conversions/06-UI-SPEC.md
  </read_first>
  <action>
    1. Update quotation/[id]/detail-actions.tsx (per QUOT-06, D-01):
    - ADD conversion buttons AFTER existing status buttons:
    - Visual separator: `<div className="border-l h-6 mx-1 hidden sm:block" />`
    - "สร้างใบแจ้งหนี้" button:
      - Visible when effectiveStatus === "accepted"
      - Implementation: `<form action={convertQuotationToInvoiceAction}><input type="hidden" name="sourceDocumentId" value={documentId} /><Button type="submit">...</Button></form>`
      - FileText icon (h-4 w-4 mr-2), variant="default"
      - Import convertQuotationToInvoiceAction from "@/app/(app)/apps/invoice/actions"
    - "สร้างใบส่งของ" button:
      - Visible when effectiveStatus === "accepted" || effectiveStatus === "converted"
      - Same form pattern with createDeliveryNoteFromSourceAction
      - Truck icon (h-4 w-4 mr-2), variant="outline"
      - Import createDeliveryNoteFromSourceAction from "@/app/(app)/apps/delivery-note/actions"
    - Both buttons show Loader2 spinner while submitting (use useFormStatus or isPending state)
    - Error handling: toast on failure (success redirects away per D-01)
    - Keep ALL existing status transition buttons unchanged per D-22

    2. Create payment-progress-bar.tsx (per UI spec Partial Payment Progress Bar):
    - "use client" component
    - Props: totalAmount (satang), paidAmount (satang), isOverdue (boolean), overdueDays (number)
    - Layout:
      - Label row: flex justify-between text-xs font-medium
        - Left: "ชำระแล้ว ฿{formatted}" (or "ยังไม่ได้ชำระ" if 0, or "ชำระครบแล้ว" if 100%)
        - Right: "คงเหลือ ฿{formatted}" (or "฿0.00" if fully paid)
      - Progress bar: use shadcn Progress component, h-3
        - value = Math.min((paidAmount / totalAmount) * 100, 100)
      - Overdue text (if isOverdue): "เกินกำหนดชำระ {N} วัน" in text-red-600 dark:text-red-400, text-xs mt-1
    - Use formatCurrency from @/lib/utils for amount display

    3. Update invoice/[id]/page.tsx:
    - Replace the Payment Status Card placeholder with actual content:
      - Import PaymentProgressBar
      - Load receipts via getDocumentsBySourceId(user.id, doc.id, "RECEIPT")
      - Calculate totalPaid via sumReceiptAmountsForInvoice (or sum from loaded receipts, excluding voided)
      - Calculate overdue: effectiveStatus === "overdue", overdueDays = Math.ceil((now - dueDate) / 86400000)
      - Render PaymentProgressBar with totalAmount=doc.total, paidAmount=totalPaid, isOverdue, overdueDays
      - Below progress bar: list linked receipts as rows:
        - Each row: receipt documentNumber (Link to /apps/receipt/{id}), payment date, amount, StatusBadge
        - Use RECEIPT_STATUSES for badges

    4. Update invoice/[id]/detail-actions.tsx:
    - Wire the conversion button formActions to actual imported actions:
      - "สร้างใบเสร็จ" -> import createReceiptFromInvoiceAction from "@/app/(app)/apps/receipt/actions"
      - "สร้างใบส่งของ" -> import createDeliveryNoteFromSourceAction from "@/app/(app)/apps/delivery-note/actions"
    - Ensure the form pattern with hidden sourceDocumentId input works correctly
    - If Plan 02 used stub imports, replace them with real imports now
  </action>
  <verify>
    <automated>cd /Users/ohmmingrath/Projects/banchee && npx next build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep "convertQuotationToInvoiceAction" app/(app)/apps/quotation/\[id\]/detail-actions.tsx returns matches
    - grep "createDeliveryNoteFromSourceAction" app/(app)/apps/quotation/\[id\]/detail-actions.tsx returns matches
    - grep "สร้างใบแจ้งหนี้" app/(app)/apps/quotation/\[id\]/detail-actions.tsx returns matches
    - grep "PaymentProgressBar" app/(app)/apps/invoice/\[id\]/page.tsx returns matches
    - grep "sumReceiptAmountsForInvoice" app/(app)/apps/invoice/\[id\]/page.tsx returns matches
    - grep "ชำระครบแล้ว" app/(app)/apps/invoice/components/payment-progress-bar.tsx returns matches
    - grep "เกินกำหนดชำระ" app/(app)/apps/invoice/components/payment-progress-bar.tsx returns matches
    - grep "createReceiptFromInvoiceAction" app/(app)/apps/invoice/\[id\]/detail-actions.tsx returns matches
    - All files compile without errors
  </acceptance_criteria>
  <done>
    Quotation detail page shows "สร้างใบแจ้งหนี้" when accepted and "สร้างใบส่งของ" when accepted/converted.
    Invoice detail shows payment progress bar with paid/remaining amounts.
    Invoice detail lists linked receipts with dates, amounts, and status badges.
    All conversion button formActions wired to real server actions.
    Build compiles cleanly.
  </done>
</task>

</tasks>

<verification>
cd /Users/ohmmingrath/Projects/banchee && npx next build 2>&1 | tail -5
ls app/(app)/apps/documents/page.tsx app/(app)/apps/documents/components/chain-badges.tsx
</verification>

<success_criteria>
- /apps/documents page shows all document types with chain badges and filters
- Chain badges are clickable Links to each document's detail page
- Type and status filters work client-side
- Quotation detail has conversion buttons for invoice and delivery note
- Invoice detail has payment progress bar with receipt list
- Existing /apps/quotation list unchanged per D-22
</success_criteria>

<output>
After completion, create `.planning/phases/06-document-workflow-chain-conversions/06-04-SUMMARY.md`
</output>
