---
phase: 06-document-workflow-chain-conversions
plan: 05
type: execute
wave: 5
depends_on: ["06-04"]
files_modified: []
autonomous: false
requirements: [QUOT-06, DOC-02, DOC-03, DOC-04, DOC-05, DOC-06, DOC-08]
must_haves:
  truths:
    - "Full document chain works end-to-end: quotation -> invoice -> receipt"
    - "Delivery notes can be created from quotation or invoice"
    - "Unified document list shows all types with chain badges"
    - "All PDFs render correctly with THSarabunNew"
    - "Partial payment tracking works correctly"
    - "Source documents are never mutated beyond status change"
  artifacts: []
  key_links: []
---

<objective>
Visual verification of the complete document workflow chain. User confirms all document conversions, PDF rendering, unified list, and payment tracking work correctly end-to-end.

Purpose: Catch visual/functional issues that automated builds cannot detect -- layout problems, Thai text rendering, conversion flow UX, PDF content accuracy.
Output: User confirmation that Phase 6 is complete and working.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-document-workflow-chain-conversions/06-CONTEXT.md
@.planning/phases/06-document-workflow-chain-conversions/06-UI-SPEC.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Visual verification: Full document workflow chain</name>
  <files>None -- verification only</files>
  <action>
    Start dev server if not running: cd /Users/ohmmingrath/Projects/banchee and npm run dev.
    Present the 7-test verification checklist to the user and wait for confirmation.
    Dev server URL: http://localhost:7331

    Test 1 -- Quotation to Invoice Conversion (QUOT-06):
    Go to /apps/quotation, find/create a quotation, progress to "accepted", click "สร้างใบแจ้งหนี้", verify redirect + data copy + quotation marked "converted" + INV-BBBB-NNNN format.

    Test 2 -- Invoice Standalone Creation (DOC-02):
    Go to /apps/invoice, create invoice with line items/contact/due date, verify "วันครบกำหนดชำระ" field and correct totals.

    Test 3 -- Invoice to Receipt (DOC-03):
    On invoice detail (status "sent"), click "สร้างใบเสร็จ", verify defaults (transfer/today/full amount), edit fields, confirm receipt, check invoice shows "paid".

    Test 4 -- Partial Payment (D-11):
    Create invoice, send it, create receipt with partial amount, verify invoice NOT "paid", verify progress bar, create second receipt for remainder, verify invoice "paid".

    Test 5 -- Delivery Note (DOC-04):
    From quotation/invoice detail, click "สร้างใบส่งของ", verify items only (no financials), verify source status unchanged.

    Test 6 -- PDF Downloads (DOC-06):
    Download Invoice PDF ("ใบแจ้งหนี้ / INVOICE" + due date), Receipt PDF ("ใบเสร็จรับเงิน / RECEIPT" + payment info), Delivery Note PDF ("ใบส่งของ / DELIVERY NOTE" no totals).

    Test 7 -- Unified Document List (DOC-08):
    Go to /apps/documents, verify all types in one table, chain badges clickable, type/status filters work, /apps/quotation unchanged.
  </action>
  <verify>
    <automated>cd /Users/ohmmingrath/Projects/banchee && npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>User confirms all 7 test scenarios pass. Phase 6 is visually verified and complete.</done>
</task>

</tasks>

<verification>
User confirms all 7 test scenarios pass.
</verification>

<success_criteria>
- All document conversions work end-to-end
- PDFs render correctly with THSarabunNew for all document types
- Unified document list shows chain badges and filters work
- Partial payment tracking correctly sums receipts
- Source documents never mutated beyond status changes
</success_criteria>

<output>
After completion, create `.planning/phases/06-document-workflow-chain-conversions/06-05-SUMMARY.md`
</output>
