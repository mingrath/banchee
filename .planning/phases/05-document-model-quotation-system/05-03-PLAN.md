---
phase: 05-document-model-quotation-system
plan: 03
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - app/(app)/apps/quotation/components/quotation-pdf.tsx
  - app/(app)/apps/quotation/components/status-badge.tsx
autonomous: true
requirements: [QUOT-04, QUOT-05]

must_haves:
  truths:
    - "Quotation PDF renders with THSarabunNew font showing company header, buyer info, line items table with unit + discount columns, totals, validity note, and 2 signature lines"
    - "StatusBadge component maps all 7 quotation statuses to correct Thai labels and semantic colors"
  artifacts:
    - path: "app/(app)/apps/quotation/components/quotation-pdf.tsx"
      provides: "@react-pdf/renderer document template for quotation"
      exports: ["QuotationPDF"]
      min_lines: 100
    - path: "app/(app)/apps/quotation/components/status-badge.tsx"
      provides: "Reusable status badge component for quotation list and detail pages"
      exports: ["StatusBadge"]
  key_links:
    - from: "app/(app)/apps/quotation/components/quotation-pdf.tsx"
      to: "exports/pdf/fonts.ts"
      via: "registerThaiFonts() call at module top level"
      pattern: "registerThaiFonts"
    - from: "app/(app)/apps/quotation/components/quotation-pdf.tsx"
      to: "services/document-workflow.ts"
      via: "QuotationData type import"
      pattern: "import.*QuotationData"
    - from: "app/(app)/apps/quotation/components/status-badge.tsx"
      to: "services/document-workflow.ts"
      via: "QUOTATION_STATUSES import for label/color lookup"
      pattern: "QUOTATION_STATUSES"
---

<objective>
Build the quotation PDF template and the reusable StatusBadge component. These are independent of the server actions and can be built in parallel with Plan 02, since they only depend on types from Plan 01.

Purpose: The PDF template is the primary output artifact for quotations (QUOT-04). The StatusBadge is shared by both list and detail views (QUOT-05).
Output: Two components ready for integration in Plan 04.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-document-model-quotation-system/05-CONTEXT.md
@.planning/phases/05-document-model-quotation-system/05-RESEARCH.md
@.planning/phases/05-document-model-quotation-system/05-UI-SPEC.md
@.planning/phases/05-document-model-quotation-system/05-01-SUMMARY.md

<interfaces>
<!-- PDF infrastructure from existing codebase -->

From exports/pdf/fonts.ts:
```typescript
export function registerThaiFonts(): void
// Registers THSarabunNew normal + bold for @react-pdf/renderer
```

From exports/pdf/thai-pdf-styles.ts:
```typescript
export const thaiPdfStyles: {
  page: object      // A4, paddingTop 30, paddingBottom 30, paddingHorizontal 40
  title: object     // 20pt bold centered
  heading: object   // 12pt bold
  // ... other shared styles
}
```

From services/document-workflow.ts (Plan 01):
```typescript
export type QuotationData = {
  id: string
  documentNumber: string
  status: string
  issuedAt: string
  validUntil: string
  paymentTerms: string
  seller: { name: string; taxId: string; branch: string; address: string; logo?: string }
  buyer: { name: string; taxId: string; branch: string; address: string }
  items: QuotationLineItem[]
  subtotal: number        // satang
  discountAmount: number  // satang
  includeVat: boolean
  vatAmount: number       // satang
  total: number           // satang
  note?: string
}

export type QuotationLineItem = {
  description: string; quantity: number; unit: string
  unitPrice: number; discount: number; amount: number  // all satang
}

export const QUOTATION_STATUSES = {
  draft: { label: "\u0e41\u0e1a\u0e1a\u0e23\u0e48\u0e32\u0e07", color: "secondary" },
  sent: { label: "\u0e2a\u0e48\u0e07\u0e41\u0e25\u0e49\u0e27", color: "blue" },
  accepted: { label: "\u0e2d\u0e19\u0e38\u0e21\u0e31\u0e15\u0e34", color: "green" },
  rejected: { label: "\u0e1b\u0e0f\u0e34\u0e40\u0e2a\u0e18", color: "destructive" },
  expired: { label: "\u0e2b\u0e21\u0e14\u0e2d\u0e32\u0e22\u0e38", color: "orange" },
  converted: { label: "\u0e41\u0e1b\u0e25\u0e07\u0e41\u0e25\u0e49\u0e27", color: "purple" },
  voided: { label: "\u0e22\u0e01\u0e40\u0e25\u0e34\u0e01", color: "muted" },
} as const
```

From services/tax-calculator.ts:
```typescript
export function formatSatangToDisplay(satang: number): number  // divides by 100
```

From lib/utils.ts:
```typescript
export function formatCurrency(total: number, currency: string)  // total in satang, divides by 100
```

From components/ui/badge.tsx:
```typescript
// Badge component with variants: default, secondary, destructive, outline
// Status colors applied via className overrides per UI-SPEC
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Quotation PDF template with THSarabunNew</name>
  <files>
    app/(app)/apps/quotation/components/quotation-pdf.tsx
  </files>
  <read_first>
    app/(app)/apps/tax-invoice/components/tax-invoice-pdf.tsx,
    exports/pdf/fonts.ts,
    exports/pdf/thai-pdf-styles.ts,
    services/document-workflow.ts,
    services/tax-calculator.ts
  </read_first>
  <action>
    Create the QuotationPDF component following the exact pattern from tax-invoice-pdf.tsx.

    **CRITICAL: Call registerThaiFonts() at module top level** (line ~5, before any component). This prevents Pitfall 6 (Thai text rendering as empty boxes).

    The component receives `{ data: QuotationData }` props.

    **Use formatSatangToDisplay() for all amount rendering** -- pass satang values directly. Do NOT divide by 100 manually. This prevents Pitfall 1 (satang double-division).

    **Format amounts for display using Intl.NumberFormat:**
    ```typescript
    function formatAmount(satang: number): string {
      return new Intl.NumberFormat("th-TH", { minimumFractionDigits: 2, maximumFractionDigits: 2 }).format(satang / 100)
    }
    ```

    **PDF Layout (per UI-SPEC PDF Layout Contract, A4 portrait):**

    Page margins: `paddingTop: 30, paddingBottom: 30, paddingHorizontal: 40`

    Sections in order:
    1. **Company header** (centered):
       - Logo image from `data.seller.logo` via `{BASE_URL}/files/static/{filename}` URL if exists, else skip
       - Company name (20pt bold)
       - Company address (11pt)
       - Tax ID: XXXXXXXXXXXXX (11pt)
       - Branch badge "\u0e2a\u0e33\u0e19\u0e31\u0e01\u0e07\u0e32\u0e19\u0e43\u0e2b\u0e0d\u0e48" or branch name (10pt)

    2. **Document title** (centered):
       - "\u0e43\u0e1a\u0e40\u0e2a\u0e19\u0e2d\u0e23\u0e32\u0e04\u0e32 / QUOTATION" (20pt bold)
       - "(\u0e15\u0e49\u0e19\u0e09\u0e1a\u0e31\u0e1a)" (14pt normal)

    3. **Document info row** (flexDirection: "row", justifyContent: "space-between"):
       - Left: "\u0e40\u0e25\u0e02\u0e17\u0e35\u0e48: {documentNumber}"
       - Right: "\u0e27\u0e31\u0e19\u0e17\u0e35\u0e48: {issuedAt formatted with formatThaiDateLong}"

    4. **Horizontal rule** (borderBottom: 1, borderColor: "#d1d5db", marginVertical: 8)

    5. **Party boxes** (flexDirection: "row", gap: 20):
       - Left box: "\u0e1c\u0e39\u0e49\u0e40\u0e2a\u0e19\u0e2d\u0e23\u0e32\u0e04\u0e32" heading (12pt bold), seller name, address, Tax ID, branch (all 11pt)
       - Right box: "\u0e1c\u0e39\u0e49\u0e23\u0e31\u0e1a\u0e23\u0e32\u0e04\u0e32" heading (12pt bold), buyer name, address, Tax ID, branch (all 11pt)

    6. **Line items table:**
       - Header row with dark background (#1f2937, white text, 10pt bold):
         Columns: # (30px center) | \u0e23\u0e32\u0e22\u0e25\u0e30\u0e40\u0e2d\u0e35\u0e22\u0e14 (flex:1 left) | \u0e08\u0e33\u0e19\u0e27\u0e19 (50px right) | \u0e2b\u0e19\u0e48\u0e27\u0e22 (50px center) | \u0e23\u0e32\u0e04\u0e32/\u0e2b\u0e19\u0e48\u0e27\u0e22 (80px right) | \u0e2a\u0e48\u0e27\u0e19\u0e25\u0e14 (70px right) | \u0e08\u0e33\u0e19\u0e27\u0e19\u0e40\u0e07\u0e34\u0e19 (90px right)
       - Data rows (11pt normal, alternating bg #f9fafb / white, borderBottom #e5e7eb):
         Map data.items with index+1 as row number

    7. **Totals section** (alignItems: "flex-end", marginTop: 12):
       - "\u0e21\u0e39\u0e25\u0e04\u0e48\u0e32\u0e2a\u0e34\u0e19\u0e04\u0e49\u0e32/\u0e1a\u0e23\u0e34\u0e01\u0e32\u0e23": subtotal (11pt)
       - "\u0e2a\u0e48\u0e27\u0e19\u0e25\u0e14\u0e23\u0e27\u0e21": discountAmount (11pt) -- only show if discountAmount > 0
       - "\u0e20\u0e32\u0e29\u0e35\u0e21\u0e39\u0e25\u0e04\u0e48\u0e32\u0e40\u0e1e\u0e34\u0e48\u0e21 7%": vatAmount (11pt) -- only show if includeVat is true
       - Separator line
       - "\u0e23\u0e27\u0e21\u0e17\u0e31\u0e49\u0e07\u0e2a\u0e34\u0e49\u0e19": total (13pt bold) + " \u0e1a\u0e32\u0e17" suffix

    8. **Validity note** (11pt, marginTop: 16):
       - "\u0e43\u0e1a\u0e40\u0e2a\u0e19\u0e2d\u0e23\u0e32\u0e04\u0e32\u0e19\u0e35\u0e49\u0e21\u0e35\u0e2d\u0e32\u0e22\u0e38 {validityDays} \u0e27\u0e31\u0e19 (\u0e16\u0e36\u0e07\u0e27\u0e31\u0e19\u0e17\u0e35\u0e48 {validUntil formatted})"
       - Calculate validityDays by diffing validUntil and issuedAt

    9. **Note** (if data.note exists, 11pt, marginTop: 8):
       - "\u0e2b\u0e21\u0e32\u0e22\u0e40\u0e2b\u0e15\u0e38: {data.note}"

    10. **Signature area** (flexDirection: "row", marginTop: 40, justifyContent: "space-around"):
        - Left block: 100px line, "\u0e1c\u0e39\u0e49\u0e40\u0e2a\u0e19\u0e2d\u0e23\u0e32\u0e04\u0e32" label, date slots (11pt)
        - Right block: 100px line, "\u0e1c\u0e39\u0e49\u0e2d\u0e19\u0e38\u0e21\u0e31\u0e15\u0e34" label, date slots (11pt)

    11. **Footer** (9pt, color #6b7280, textAlign center, marginTop: 20):
        - "\u0e2a\u0e23\u0e49\u0e32\u0e07\u0e42\u0e14\u0e22 BanChee \u2014 \u0e40\u0e2d\u0e01\u0e2a\u0e32\u0e23\u0e19\u0e35\u0e49\u0e40\u0e1b\u0e47\u0e19\u0e40\u0e2d\u0e01\u0e2a\u0e32\u0e23\u0e2d\u0e49\u0e32\u0e07\u0e2d\u0e34\u0e07"

    Use StyleSheet.create for all styles. Import shared styles from `@/exports/pdf/thai-pdf-styles` and extend with quotation-specific styles.
  </action>
  <verify>
    <automated>cd /Users/ohmmingrath/Projects/banchee && grep -c "registerThaiFonts" app/\(app\)/apps/quotation/components/quotation-pdf.tsx && grep -c "formatAmount" app/\(app\)/apps/quotation/components/quotation-pdf.tsx && echo "PDF component checks passed"</automated>
  </verify>
  <acceptance_criteria>
    - quotation-pdf.tsx contains "import { registerThaiFonts }"
    - quotation-pdf.tsx contains "registerThaiFonts()" called at module top level (not inside component)
    - quotation-pdf.tsx contains "export function QuotationPDF" or "export default function QuotationPDF"
    - quotation-pdf.tsx contains "QuotationData" (props type)
    - quotation-pdf.tsx contains "Document" and "Page" from @react-pdf/renderer
    - quotation-pdf.tsx contains "StyleSheet.create"
    - quotation-pdf.tsx contains "/ 100" inside formatAmount (satang to baht display)
    - quotation-pdf.tsx does NOT contain "formatCurrency" (uses local formatAmount for PDF -- formatCurrency adds currency symbol which conflicts with PDF layout)
    - quotation-pdf.tsx contains 7 column headers in Thai for the items table
    - quotation-pdf.tsx contains signature labels for both proposer and approver
    - quotation-pdf.tsx contains "BanChee" in footer text
  </acceptance_criteria>
  <done>
    QuotationPDF renders complete A4 PDF matching UI-SPEC layout: company header with optional logo, document title, party boxes, 7-column line items table, totals with conditional discount and VAT rows, validity note, note section, 2 signature lines, and BanChee footer. THSarabunNew registered at module level. All amounts rendered from satang via formatAmount().
  </done>
</task>

<task type="auto">
  <name>Task 2: StatusBadge component for quotation statuses</name>
  <files>
    app/(app)/apps/quotation/components/status-badge.tsx
  </files>
  <read_first>
    components/ui/badge.tsx,
    services/document-workflow.ts
  </read_first>
  <action>
    Create a reusable StatusBadge component that maps a status string to the correct Thai label and semantic color.

    This is a client component ("use client") since it may be used inside client components.

    ```tsx
    "use client"

    import { Badge } from "@/components/ui/badge"
    import { QUOTATION_STATUSES } from "@/services/document-workflow"

    const STATUS_STYLES: Record<string, string> = {
      draft: "bg-secondary text-secondary-foreground",
      sent: "bg-blue-100 dark:bg-blue-900/30 text-blue-700 dark:text-blue-300",
      accepted: "bg-green-100 dark:bg-green-900/30 text-green-700 dark:text-green-300",
      rejected: "bg-destructive/10 text-destructive",
      expired: "bg-orange-100 dark:bg-orange-900/30 text-orange-700 dark:text-orange-300",
      converted: "bg-purple-100 dark:bg-purple-900/30 text-purple-700 dark:text-purple-300",
      voided: "bg-muted text-muted-foreground",
    }

    export function StatusBadge({ status }: { status: string }) {
      const statusInfo = QUOTATION_STATUSES[status as keyof typeof QUOTATION_STATUSES]
      const label = statusInfo?.label ?? status
      const className = STATUS_STYLES[status] ?? "bg-secondary text-secondary-foreground"

      return (
        <Badge variant="outline" className={className}>
          {label}
        </Badge>
      )
    }
    ```

    The `variant="outline"` base ensures the Badge has a clean border, while `className` override applies the semantic background and text colors per the UI-SPEC status badge color table.
  </action>
  <verify>
    <automated>cd /Users/ohmmingrath/Projects/banchee && grep -c "StatusBadge" app/\(app\)/apps/quotation/components/status-badge.tsx && grep -c "QUOTATION_STATUSES" app/\(app\)/apps/quotation/components/status-badge.tsx && echo "StatusBadge checks passed"</automated>
  </verify>
  <acceptance_criteria>
    - status-badge.tsx contains '"use client"'
    - status-badge.tsx contains "export function StatusBadge("
    - status-badge.tsx contains "import { Badge }"
    - status-badge.tsx contains "import { QUOTATION_STATUSES }"
    - status-badge.tsx contains "bg-blue-100" (sent status)
    - status-badge.tsx contains "bg-green-100" (accepted status)
    - status-badge.tsx contains "bg-orange-100" (expired status)
    - status-badge.tsx contains "bg-purple-100" (converted status)
    - status-badge.tsx contains "bg-destructive/10" (rejected status)
    - status-badge.tsx contains "bg-muted" (voided status)
  </acceptance_criteria>
  <done>
    StatusBadge renders correct Thai label and semantic color for all 7 quotation statuses: draft (secondary), sent (blue), accepted (green), rejected (destructive), expired (orange), converted (purple), voided (muted). Supports dark mode via dark: variants. Reusable by Phase 6 for other document types.
  </done>
</task>

</tasks>

<verification>
```bash
# PDF component has font registration
grep "registerThaiFonts()" app/\(app\)/apps/quotation/components/quotation-pdf.tsx

# StatusBadge exports correctly
grep "export function StatusBadge" app/\(app\)/apps/quotation/components/status-badge.tsx

# No direct division by 100 in PDF (uses formatAmount helper)
grep -c "/ 100" app/\(app\)/apps/quotation/components/quotation-pdf.tsx  # should be exactly 1 (inside formatAmount)
```
</verification>

<success_criteria>
- QuotationPDF renders complete Thai quotation per D-12, D-13 layout specification
- THSarabunNew registered at module level (Pitfall 6 prevention)
- All PDF amounts use satang-to-baht conversion via formatAmount helper (Pitfall 1 prevention)
- PDF has 7-column table matching UI-SPEC column widths
- PDF has conditional discount and VAT rows based on data values
- PDF has validity note with computed days and formatted date
- PDF has 2 signature blocks (proposer + approver)
- StatusBadge handles all 7 statuses with correct Thai labels and colors per UI-SPEC
- StatusBadge supports dark mode
</success_criteria>

<output>
After completion, create `.planning/phases/05-document-model-quotation-system/05-03-SUMMARY.md`
</output>
